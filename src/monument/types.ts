export type Monument = {
  id: string;
  name: string;
  description: string;
  imageUrl: string;
  country: string;
  city: string;
};

export type MonumentCommonData = {
  name: string;
  description: string;
  imageUrl: string;
  country: string;
  city: string;
};
